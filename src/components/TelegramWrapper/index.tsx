"use client"
import React from 'react';
// Remove useState if it's not being used
import TelegramContainer from '../TelegramContainer';

export default function TelegramWrapper() {
  return (
    <TelegramContainer />
  );
} 